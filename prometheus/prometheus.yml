global:
  #prometheus bate no metrics de cada serviço a cada 15seg
  scrape_interval: 15s

scrape_configs:
  - job_name: "http-server-projeto-korp"
    static_configs:
    - targets: ["http-server-projeto-korp:8080"]